#include <stdio.h>
#include <stdlib.h>

int main(int argc, char * argv){
    int x = 1;
    int i = 0;
    for(;i<20; i++){

    if(x==1){
        exit(0);
    }
        puts("Inconcieveable!");
    }

    puts("I do not think that word means what you think it means.");
}
